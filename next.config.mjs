/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  eslint: {
    // ignoreDuringBuilds: true, // Dihapus
  },
  typescript: {
    // ignoreBuildErrors: true, // Dihapus
  },
};

export default nextConfig;